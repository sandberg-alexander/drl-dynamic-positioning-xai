# Dockerfile.xai
FROM my-ros-base:latest

# Install milliampere_xai package (milliampere_dp already at /app/milliampere_dp/ from base)
WORKDIR /app
COPY xai/ /app/xai/
RUN uv pip install --system --no-cache --no-sources /app/milliampere_dp/ /app/xai/

CMD ["tail", "-f", "/dev/null"]
