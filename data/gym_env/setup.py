# /app/gym_env/setup.py
from setuptools import setup

# This setup.py is minimal and relies on pyproject.toml for actual build config
# It's primarily here to satisfy older pip/setuptools versions expecting it for editable installs.
setup()